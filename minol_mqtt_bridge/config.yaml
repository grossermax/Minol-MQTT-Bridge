name: "Minol MQTT Bridge"
description: "Fetches Minol consumption data via Playwright and sends it to MQTT"
version: "1.1.1" # x-release-please-version
slug: "minol_mqtt"
init: false
arch:
  - aarch64
  - amd64
  - armhf
  - armv7
  - i386
options:
  minol_email: "user@email.com"
  minol_password: "password"
  mqtt_host: "core-mosquitto"
  mqtt_port: 1883
  mqtt_user: "mqtt_user"
  mqtt_password: "mqtt_password"
  scan_interval_hours: 12
  base_url: "https://webservices.minol.com"
  consumption_types:
    - HEIZUNG
  log_level: "INFO"
schema:
  minol_email: str
  minol_password: str
  mqtt_host: str
  mqtt_port: int
  mqtt_user: str
  mqtt_password: str
  scan_interval_hours: int
  base_url: str
  consumption_types:
    - list(HEIZUNG|WARMWASSER|KALTWASSER)
  log_level: list(DEBUG|INFO|WARNING|ERROR|CRITICAL)